import { createContext } from 'react';
// import { SidebarMenuItem, sidebarMenuData } from '../types/Sidebar.types';

export const SidebarContext = createContext(null);
export const SidebarDispatch = createContext(null);
